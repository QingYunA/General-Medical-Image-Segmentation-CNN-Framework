name: unet3d
ckpt: "/disk/cyq/2025/General-Medical-Image-Segmentation-CNN-Framework/logs/unet3d/pl_train-2025-06-12/11-22-08/train-epoch00-dice0.60.ckpt"

output_dir : "./logs/${config.name}"

model:
  in_channels: 1
  out_channels: 2
  init_features: 32


##### if you want to replace the default data and trainer, you can use the following code
# data:
#   source_path: "./data/train/images"
# trainer:
#   max_epochs: 10